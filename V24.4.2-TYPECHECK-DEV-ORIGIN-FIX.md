# OpiOpe V24.4.2 — Typecheck and development-origin fix

- Fixed TS2367 in `AccountRegistrationForm.tsx` by computing course labels before rendering.
- Added local development origins for both previously used LAN addresses, localhost, and 127.0.0.1.
- Limited COOP/CORP/HSTS headers to production so plain HTTP LAN development does not emit trustworthy-origin COOP warnings.
- No production authentication or security model was weakened; production keeps the security headers.

