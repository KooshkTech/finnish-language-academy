# V23.1 Lint Fix

Fixes based on the local V23 test log:

- Removed synchronous state resets from a `useEffect` in `InteractiveQALoop.tsx`; question state is now reset explicitly when advancing to the next question.
- Renamed the reserved local variable `module` to `lessonModule` in `StandaloneQASkillPage.tsx`.
- Reworked initial Teacher Lesson Calendar loading so state updates happen from the asynchronous fetch completion callback rather than a synchronously invoked state-setting function inside the effect.

Local workspace warning note: if an accidental `package-lock.json` exists one directory above the actual app, remove that outer lockfile. The app's own lockfile should remain next to its `package.json`.

