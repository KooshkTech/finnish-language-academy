# OpiOpe V24.6 — production-finalization pass

This pass keeps the two-course architecture (Suomi / Ruotsi) and removes known learner-facing English strings from the principal legacy learning tools that were still reachable.

## Changed
- Finnish labels for dictionary, voice lab, schedule, realtime quiz, assessment report and universal tools.
- Removed the visible English model-answer row from interactive Q&A.
- Finnishized legacy Academy headings and actions.
- Finnishized old `/learn` Academy promotion and contact label.
- Added `npm run audit:language` to catch selected English UI phrases before release.
- Preserved English only where it is technical/internal (API prompts, variable names, code comments, provider payloads) rather than learner UI.

## External production dependencies still required
A code ZIP cannot provide real production credentials or infrastructure. Before launch, configure Supabase keys, apply migrations, configure email redirects, establish the first admin account, test RLS/IDOR isolation, verify teacher MFA/approval, deploy over HTTPS, and run lint/typecheck/build plus the language audit.

