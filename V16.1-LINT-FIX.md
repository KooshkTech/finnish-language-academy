# OpiOpe V16.1 — Lint Fix

Fixed the lint findings reported from the local Windows build:

- Replaced internal `/courses` anchors with `next/link`.
- Rewrote the mobile practice handler to avoid an unused ternary expression.
- Removed `Date.now()` from React render initialization in `LiveQuizLobby`.
- Removed the synchronous live-room refresh from the subscription effect.
- Reworked `LearningProfileSwitcher` initialization and DOM synchronization to satisfy React purity/immutability rules.
- Reworked dictionary query initialization so URL state is read by lazy state initialization instead of synchronous state writes in an effect.
- Renamed the local `module` loop variable in `data/course-catalog.ts`.

The user's V16 run already showed:
- `npm install`: completed, 0 vulnerabilities
- `npm run typecheck`: passed
- `npm run build`: passed
- `npm run dev`: ready

Re-run lint/typecheck/build locally to verify this patch.

