# OpiOpe V24.6.3 — Sign-out Router Fix

- Replaced internal `window.location.assign('/')` navigation in `components/auth/SignOutButton.tsx` with Next.js `useRouter()` navigation.
- Uses `router.replace('/')` after sign-out so the signed-in page is not kept as the immediate browser back destination.
- Calls `router.refresh()` after a configured Supabase sign-out so server-rendered auth state is refreshed.
- Removes the two `@next/next/no-location-assign-relative-destination` lint warnings reported in V24.6.2.

